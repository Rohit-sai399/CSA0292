#include <stdio.h>
#include<string.h>

int main()
{
 char s[]={"coffee is awesome"};
 int i,j,temp;
 int len=strlen(s);
 
for(i=0,j=len-1;i<j;i++,j--)
{
         temp=s[i];
         s[i]=s[j];
         s[j]=temp;
     }
     
 
 printf("sorted string=%s",s);
}

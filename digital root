#include<stdio.h>
int main()
{
    int num=97572;
    int rem,sum=0;
    while(num>=10)
    {
        sum=0;
        while(num!=0)
        {
            rem=num%10;
            sum=sum+rem;
            num/=10;
        }
        num=sum;
    }
    printf("sum=%d",num);
}

#include<stdio.h>
int main(){
    int num=5,count,i;
    i=1;
    while(i<=num)
    {
        if(num%i==0)
        count++;
        i++;
    }
    if(count=2)
    {
        printf("prime");
        
    }
    else
    ("not prime");
}

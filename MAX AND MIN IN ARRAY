#include <stdio.h>
int main()
{
    int i,a[6]={1,2,3,55,34,5},max=a[0],min=a[0];
    for(i=0;i<6;i++)
    {
        if(a[i]>max)
        {
            max=a[i];
        }
        if(a[i]<min)
        {
            min=a[i];
        }
    }
     
     printf("max and min values are %d,%d",max,min);


    return 0;
}

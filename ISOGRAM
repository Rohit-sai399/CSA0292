#include<stdio.h>
#include <string.h>
int main()
{
 char str1[100]="BALLASTIC";
 char str2[100]="CHRONIC";
 char ch,temp;
 int i,j, len1,len2;
 len1 = strlen(str1);
 len2 = strlen(str2);
 if(len1 != len2)
 {
 printf("given strings are not isograms");
 }
 else
 {
 printf("given strings are isograms");
 }
}


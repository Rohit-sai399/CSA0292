#include <stdio.h>
#include<string.h>

int main()
{
  char ch, s[]={"Sai Rohit is a good boy"};
  int i,count=0;
  for(i=0;s[i]!='\0';i++)
  {
      ch=s[i];
      if(ch!='\0')
      {
          count++;
      }
  }
  printf("length of string=%d",count);
}
                

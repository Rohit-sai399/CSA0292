#include<stdio.h>
int main()
{
    int num=23;
    int i,isprime=1;
    if(num<2)
    {
        isprime=0;
    }
    for(i=2;i*i<=num;i++)
    {
        if(num%i==0)
        {
            isprime=0;
        }
    }
    if(isprime==1)
    {
        printf("prime");
    }
}
